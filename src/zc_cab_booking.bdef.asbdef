projection;
strict ( 1 );
use draft;

define behavior for ZC_CAB_BOOKING alias Booking
{
  use create;
  use update;
  use delete;

  use action AssignDriver;
  use action StartTrip;
  use action CompleteTrip;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}